[Unit]
Description=Run the xResidual capture audit daily (catch a missed match the next day, not months later)

[Timer]
OnCalendar=*-*-* 05:00:00 UTC
Persistent=true

[Install]
WantedBy=timers.target
